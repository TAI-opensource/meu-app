<UserControl x:Class="CRM_Alunos.Pages.DocumentosPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="900"
             Background="#F1F5F9">

    <Grid Margin="32">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="8"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="16"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="16"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <DockPanel Grid.Row="0">
            <StackPanel VerticalAlignment="Center">
                <TextBlock Text="Documentos" FontSize="28" FontWeight="Bold" Foreground="#1E293B"/>
                <TextBlock Text="Visualize e gerencie todos os documentos" FontSize="14" Foreground="#64748B" Margin="0,4,0,0"/>
            </StackPanel>
        </DockPanel>

        <!-- Search -->
        <Border Grid.Row="4" Background="White" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="8" Padding="4">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="&#xE721;" FontFamily="Segoe MDL2 Assets" FontSize="14"
                           Foreground="#64748B" VerticalAlignment="Center" Margin="8,0,8,0"/>
                <TextBox x:Name="TxtSearch" Grid.Column="1" BorderThickness="0" Padding="8" FontSize="14"
                         Background="Transparent" KeyUp="TxtSearch_KeyUp"/>
            </Grid>
        </Border>

        <!-- DataGrid -->
        <DataGrid Grid.Row="6" x:Name="DgDocumentos" AutoGenerateColumns="False" IsReadOnly="True"
                  HeadersVisibility="Column" BorderThickness="0" Background="White"
                  RowBackground="White" AlternatingRowBackground="#F1F5F9"
                  GridLinesVisibility="None" CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Nome do Documento" Binding="{Binding Nome}" Width="*"/>
                <DataGridTextColumn Header="Tipo" Binding="{Binding Tipo}" Width="120"/>
                <DataGridTextColumn Header="Aluno" Binding="{Binding Aluno.Nome}" Width="180"/>
                <DataGridTextColumn Header="Turma" Binding="{Binding Aluno.Turma.Nome}" Width="140"/>
                <DataGridTextColumn Header="Data Upload" Binding="{Binding DataUpload, StringFormat=dd/MM/yyyy}" Width="120"/>
                <DataGridTextColumn Header="Tamanho" Binding="{Binding Tamanho}" Width="100"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
